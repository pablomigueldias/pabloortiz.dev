# Barreira local contra segredo e dado pessoal. O CI roda a mesma varredura:
# pular o hook com --no-verify não adianta.
pre-commit:
  commands:
    gitleaks:
      run: gitleaks protect --staged --redact --no-banner --config .gitleaks.toml
