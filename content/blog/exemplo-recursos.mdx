---
titulo: "Exemplo de recursos"
descricao: "Post de teste do pipeline MDX: frontmatter, GFM e componentes."
data: 2026-09-21
pilar: ia-llms
draft: true
---

# Exemplo de recursos

Este post existe para testar o pipeline. Ele **nunca** vai para produção.

## Tabela (GFM)

| Recurso | Status |
| ------- | ------ |
| MDX     | ok     |

- [x] lista de tarefas
- [ ] outro item

~~riscado~~ e https://exemplo.com vira link.

## Código

Com nome de arquivo e linhas destacadas:

```python title="busca.py" {3-4}
def buscar(consulta: str, limite: int = 5) -> list[str]:
    vetorial = busca_vetorial(consulta, limite)
    lexical = busca_full_text(consulta, limite)
    return fundir_rrf(vetorial, lexical)[:limite]
```

Código inline: `SELECT 1;`

## Matemática

Inline: a similaridade de cosseno é $\cos\theta = \frac{a \cdot b}{\|a\|\|b\|}$.

Em bloco, a fusão por Reciprocal Rank Fusion:

$$
\text{RRF}(d) = \sum_{r \in R} \frac{1}{k + r(d)}
$$
